name: Publish

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Git tag to publish (e.g. v0.1.0)"
        required: true
        type: string
      pypi:
        description: "Also publish to PyPI"
        required: false
        type: boolean
        default: false

jobs:
  resolve-tag:
    runs-on: ubuntu-24.04
    outputs:
      tag: ${{ steps.resolve.outputs.tag }}
    steps:
      - name: Resolve tag
        id: resolve
        run: echo "tag=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"

  verify-release:
    needs: resolve-tag
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
      - name: Verify GitHub Release exists
        run: gh release view "${{ needs.resolve-tag.outputs.tag }}"

  ci:
    needs: [verify-release, resolve-tag]
    uses: ./.github/workflows/ci.yml
    with:
      ref: ${{ needs.resolve-tag.outputs.tag }}

  build-wheels:
    needs: [ci, resolve-tag]
    permissions:
      contents: read
    strategy:
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
          - arch: aarch64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-tag.outputs.tag }}

      - name: Build wheels
        # Keep the cibuildwheel version and manylinux image tags below in sync with
        # .github/workflows/wheel-smoke.yml, which exercises this same release build path.
        uses: pypa/cibuildwheel@v4.1.0
        with:
          package-dir: bindings/python
          output-dir: wheelhouse
        env:
          CIBW_BUILD: "cp311-manylinux* cp312-manylinux* cp313-manylinux*"
          CIBW_SKIP: "*-musllinux*"
          CIBW_ARCHS_LINUX: ${{ matrix.arch }}
          # numpy ships only manylinux_2_28 wheels since 2.3.0; build on that baseline so the
          # build env installs a prebuilt numpy instead of source-building it.
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux_2_28
          CIBW_MANYLINUX_AARCH64_IMAGE: manylinux_2_28
          # Import-test each built wheel (deps, incl. numpy>=2.0, are installed from the wheel)
          # so we never upload a wheel that builds but can't import or run the ndarray path.
          CIBW_TEST_REQUIRES: pytest>=8.0
          CIBW_TEST_COMMAND: "pytest {package}/tests"

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.arch }}
          path: wheelhouse/*.whl

  upload-release-assets:
    needs: [build-wheels, resolve-tag]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist

      - name: Upload wheels to GitHub Release
        run: gh release upload "${{ needs.resolve-tag.outputs.tag }}" dist/*.whl --clobber

  publish-pypi:
    if: inputs.pypi
    needs: [build-wheels, upload-release-assets]
    runs-on: ubuntu-24.04
    permissions:
      id-token: write
      contents: read
    environment: pypi
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
